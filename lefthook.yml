pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: pnpm exec biome check --write {staged_files} && git add {staged_files}

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
